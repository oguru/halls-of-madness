import React from "react";
import Dashboard from "./Dashboard";
import { render } from "@testing-library/react";
import { mount, shallow } from "enzyme";
import RandomFace from "../RandomFace";

describe("Dashboard tests", () => {
  // let component;
  // beforeEach(() => {
  //   component = mount(<Dashboard number={7} />);
  // });
  // it("should render the correct number of faces", () => {
  //   // expect(component.find(RandomFace).length).toEqual(7);
  //   expect(component.find(RandomFace).length === 7).toBeFalse;
  // });
});
