import React from "react";
import { render } from "@testing-library/react";
import App from "./App";
import { mount, shallow } from "enzyme";

describe("App tests", () => {
  // let component;
  // const clickMe = require("./assets/audio/audio");
  // beforeEach(() => {
  //   component = mount(<App />);
  // });
  // it("should play a random clickme sound when playClickMe is triggered", () => {
  //   expect(shallow(<App />));
  // });
  // it("should render the faces and eyes on the page", () => {
  //   expect(component.find(randomNum).toBe(!null));
  // });
});
